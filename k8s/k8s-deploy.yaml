---
# Namespace for KubeKuma
apiVersion: v1
kind: Namespace
metadata:
  name: kubekuma

---
# Secret with test user password hash
apiVersion: v1
kind: Secret
metadata:
  name: admin-password
  namespace: kubekuma
type: Opaque
stringData:
  # Password: test1234 (min 8 chars required)
  hash: "$argon2id$v=19$m=65536,t=3,p=4$Ha7NhMrOOSle+AMHOp5XNw$jhFoCy75xBnmZJY+FKPujTeFg26xnR1wfDwFJJVrBhU"

---
# ConfigMap for environment variables
apiVersion: v1
kind: ConfigMap
metadata:
  name: kubekuma-config
  namespace: kubekuma
data:
  SESSION_SECRET: "dev-secret-change-in-production"
  NODE_ENV: "development"
  LOG_LEVEL: "info"

---
# Deployment for KubeKuma API Server
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubekuma-api
  namespace: kubekuma
  labels:
    app: kubekuma
    component: api-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: kubekuma
      app.kubernetes.io/component: api-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kubekuma
        app.kubernetes.io/component: api-server
      annotations:
        description: "KubeKuma API Server"
    spec:
      serviceAccountName: kubekuma
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: api-server
        image: kubekuma-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 3000
          protocol: TCP
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: kubekuma-config
              key: NODE_ENV
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: kubekuma-config
              key: LOG_LEVEL
        - name: SESSION_SECRET
          valueFrom:
            configMapKeyRef:
              name: kubekuma-config
              key: SESSION_SECRET
        - name: PORT
          value: "3000"
        - name: DATABASE_URL
          value: "sqlite:/data/kubekuma.db"
        - name: KUBE_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: NODE_TLS_REJECT_UNAUTHORIZED
          value: "0"

        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL

        # Health checks
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2

        # Resource limits
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"

        # Volume mounts
        volumeMounts:
        - name: data
          mountPath: /data
        - name: tmp
          mountPath: /tmp

      # Volumes
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: kubekuma-pvc
      - name: tmp
        emptyDir: {}

---
# Service to expose the API
apiVersion: v1
kind: Service
metadata:
  name: kubekuma-api
  namespace: kubekuma
  labels:
    app.kubernetes.io/name: kubekuma
    app.kubernetes.io/component: api-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app.kubernetes.io/name: kubekuma
    app.kubernetes.io/component: api-server
