[build]
root = "src"
outdir = "dist"

[test]
# Unit tests colocated with source; e2e tests run separately via `bun run test:e2e:tests`
root = "src"
coverage = true
coverageDir = "./coverage"
coverage_threshold = 80
coverage_reporter = ["text", "lcov", "html"]
